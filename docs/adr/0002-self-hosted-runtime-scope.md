# Scope Foldplane as a Self-Hosted Runtime

Foldplane is scoped as a self-hosted runtime that users run in their own Cloudflare account, not as a managed hosted service. This keeps the project distinct from Convex Cloud, reduces operational scope, and preserves focus on open runtime software even though it gives up the immediate convenience and business-model clarity of a hosted platform.
