# Stabilize Runtime Semantics Before Cloudflare Implementation

Foldplane will stabilize Runtime semantics before allowing Cloudflare-specific implementation work to define behavior. This keeps the Cloudflare Runtime as a host for the Runtime model rather than the source of that model, even though it delays platform-specific progress.
